- always use tanstack hooks for api requests for everything
- use useeffect and usestate as less as possible if 0 times the best
- dont hardcode colors always use them from the config for shadcn
- for all components use shadcn first if it exists that our first priority
- apply good structuring always
- use lib folder too for functions u think can go in it
- use minimal code as possible keeping it perfect and clean
- for shadcn components we already use the components/ui folder which is good
- for other smaller parts we repeat in code make like components/movie which holds components of the movie page
- divide into reusable components
- use skeletons for page loading animations
- use react toast to show success failure and loading api which will be trigerred from the hooks success loading and failure things in the hook itself not in the ui code the hooks code